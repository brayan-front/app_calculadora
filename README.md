# Calculadora Lógica Binaria

Este proyecto implementa una calculadora en TypeScript que realiza operaciones básicas (suma, resta, multiplicación y división) utilizando **lógica binaria** (operadores bitwise).

## 🚀 Ejecución
1. Instalar dependencias:
   ```bash
   npm install
   ```
2. Compilar:
   ```bash
   npm run build
   ```
3. Ejecutar:
   ```bash
   npm start
   ```
4. Probar:
   ```bash
   npm test
   ```
